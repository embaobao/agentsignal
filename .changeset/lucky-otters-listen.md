---
"@agentssignal/ui": patch
---

详情页 Use this Signal 复制重做：默认复制「Agent 提示词」（引导任意 Agent 经 /skills 总入口接入并对 sig 执行 use→Runbook→verify 回流），新增下拉可切换「复制为 CLI 命令」（agentsignal use <sig_id>）；新增 use-copy 纯函数模块与契约测试。另：participant SKILL 补文档 init/me/ls 三命令（G2 护栏修复 Phase 5 漂移）；新增根级 skills/ 分发镜像（G4 同步护栏）供 skills.sh（npx skills add）默认索引。
