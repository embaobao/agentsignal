---
"@agentssignal/cli": minor
---

cli: 新增 `verify <sig_id>` 第六命令（验证 +1，匿名点赞，闭环 use→执行→verify→回流）；participant SKILL 重设计为「安装引导 + 使用引导」（六命令同步、零硬编码地址/base 来源推导、零 curl、`--help` 自纠兜底）；新增 G1–G3 同步护栏测试（版本 lockstep + 命令面双向一致 + /skills 托管一致）。
