/** 侧栏：按 docs/ 目录结构自动生成（装配器已按 site-publishing-policy 白名单出内容）。 */
/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  main: [{ type: "autogenerated", dirName: "." }],
};

module.exports = sidebars;
