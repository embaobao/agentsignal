{
  "name": "@agentsignal/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env --watch src/index.ts",
    "start": "node --env-file-if-exists=../../.env src/index.ts",
    "predev": "node --env-file-if-exists=../../.env -e \"const net=require('node:net');let u;try{u=new URL(process.env.DATABASE_URL??'')}catch{console.error('[predev] DATABASE_URL 非法');process.exit(1)};if(!u.hostname){console.error('[predev] 缺少 DATABASE_URL：pnpm bootstrap 生成 .env，或自行 export');process.exit(1)};const s=net.connect({host:u.hostname,port:Number(u.port||5432),timeout:1500});s.once('connect',()=>{console.log('[predev] Postgres 可达 →',u.host+':'+(u.port||5432));s.destroy()});s.once('timeout',()=>{console.error('[predev] Postgres 连接超时 →',u.host+':'+(u.port||5432),'—— 先 pnpm db:up');process.exit(1)});s.once('error',()=>{console.error('[predev] Postgres 不可达 →',u.host+':'+(u.port||5432),'—— 先 pnpm db:up，或检查 .env 的 DATABASE_URL');process.exit(1)})\""
  },
  "dependencies": {
    "@agentsignal/audit": "workspace:*",
    "@agentsignal/protocol": "workspace:*",
    "@fastify/cookie": "^11.1.2",
    "@fastify/cors": "^11.3.0",
    "@fastify/helmet": "^13.1.1",
    "@fastify/rate-limit": "^11.2.0",
    "@fastify/static": "^10.1.3",
    "@fastify/swagger": "^9.8.1",
    "@fastify/type-provider-zod": "^1.0.0",
    "@scalar/fastify-api-reference": "^1.66.1",
    "bcryptjs": "^3.0.3",
    "fastify": "^5.12.1",
    "pg": "^8.16.0",
    "pino-pretty": "^13.1.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@types/node": "^26.4.0",
    "@types/pg": "^8.15.0",
    "light-my-request": "^6.6.0"
  },
  "description": "@agentsignal/api — Fastify 三链路服务（分享/检索/构建发布）+ Token Firewall（鉴权/限频/校验）",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/embaobao/agentsignal.git"
  },
  "author": "AgentSignal"
}
