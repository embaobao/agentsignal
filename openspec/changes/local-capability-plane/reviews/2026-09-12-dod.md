# 审查报告·DoD 与台账一致性（席4·2026-09-12）

> 审查范围：2026-09-11 23:00 后 local-capability-plane 提案落地产物（git log `--since '2026-09-11 22:00'`）。
> 审查方式：只读（零 git 写操作）。对账工具：`node scripts/check-ledger.mjs`（本夜 C3 产物，已实跑）。
> 工作区状态：`git status --porcelain` 干净（零未提交产物），符合审查协议 §0.5 前提。

## 一、当夜提交清单（spec=local-capability-plane）

| sha | 类型 | 任务 |
|---|---|---|
| `cd3dc12` | feat | 0.1 定位扩展入册 |
| `b0d9bb0` | feat | 0.2 产物 frontmatter schema |
| `7e83c1d` | feat | 0.3 落库产物首部 |
| `fec54b5` | chore | protocol dist 随 0.2 重建 + 夜间轮1工作记忆（0.2/0.3 尾注） |
| `f76f666` | feat | 5.1 verify_target 补实现 |
| `2653873` | feat | 5.2 provenance.origin 补写入 |
| `b31187d` | feat | C1 场景测试骨架 |
| `59e7834` | feat | C3 台账一致性脚本 |

## 二、逐任务 DoD 对照

| 任务 | 勾 | 结论 | 证据（文件:行 / sha） |
|---|---|---|---|
| **0.1** AGENTS.md 定位更新 | [x] | ✅ | `AGENTS.md:8` D1/D2 定位扩展行（经验层+本地能力面，引决议 2026-09-11）；`AGENTS.md:59` 当前阶段刷 local-capability-plane P0；`AGENTS.md:63` 剩余节列当前主线（0/59）；`docs/README.md:3` 进度行 0.5.0 口径 + P0 主线。三节与决议一致。sha `cd3dc12` |
| **0.2** 产物 frontmatter schema | [x] | ✅ | `packages/protocol/src/skill-schema.ts:183` `ArtifactFrontmatterSchema`（仅 name/description，均必填）；`:189` `parseArtifactFrontmatter`（name≠目录名抛错）；`:285` `ArtifactFrontmatter` 类型导出。**私有层字段核验**：schema 体（:183-186）确无 `layers`/`triggers`/`domains`，仅存在于内部形式 `SkillFrontmatterSchema`（:144-148），注释（:179-181）明确零泄漏语义。sha `b0d9bb0` |
| **0.3** 落库生成/校验 frontmatter | [x] | ✅ | `packages/cli/src/skills/transcoder.ts:96` `renderArtifactSkillMd`（首部仅两字段，JSON.stringify 序列化 YAML 安全，写前过 parseArtifactFrontmatter）；`:110` `stripArtifactFrontmatter`（精确正则匹配本族输出，旧技能/`---` 水平线正文原样返回=零破坏）；`install.ts:14,41-44` 落库接产物首部；`loader.ts:16,132-133` 注入侧剥离（Token Firewall）。私有增强只在 skill.json5（transcoder.ts:91-92）。sha `7e83c1d` |
| **0.4** 快照测试 | [ ] | ⬜ 未开工（一致） | 无对应提交、无夹具。台账未勾与 git log 一致，非违规。**提示**：0.2/0.3 旧技能零破坏目前靠正则防御性设计 + 既有断言，正/旧两版样例夹具待 0.4 落地闭环 |
| **0.5** ★门 G-A | [ ] | ⬜ 门未动（正确） | 按纪律不编码，待站长真机抽验 |
| **5.1** verify_target 补实现 | [x] | ✅ | `skill-schema.ts:132` `SkillVerifyTargetSchema({statement, checks[]})`；`:159` union 收宽（新对象/旧 `z.array(z.string())`/旧 `null`）；`verify.ts:16` `normalizeVerifyTarget` 引擎层归一（null/undefined→null；数组→`{statement:首条, checks}`，空数组→null；对象校验）；`verify.ts:35` `VerifyResult.verify_target`；`:67` 判定结果带归一目标。旧数据零破坏口径成立。sha `f76f666` |
| **5.2** provenance.origin 补写入 | [x] | ✅ | `transcoder.ts:45` `TranscodeInput.origin` 声明位；`:145` 缺省 `{kind:"platform", ref: input.id}`（input.id 即信封 sig id，与 `:143` `sig_id` 同源），新落库必有 origin；包导入/手动路径留显式透传接线位（P4）。旧记录 optional 兼容。sha `2653873` |
| **C1** 场景测试骨架 | [x] | ✅ | `packages/cli/test/scenarios/` 17 文件 = **S1–S16 十六壳**（s01-golden-path … s16-per-agent-mgmt，逐场景对 design §8.5）+ `fixtures.ts`。壳形态 `test.todo` 可跳过待填充（s01:14 实证）；fixtures 构造器 6 个（makeScenarioRoot/seedHostHome/seedHostSkillsDir/seedForeignSkill/seedTwoHosts/cleanupScenarioRoot），覆盖 DoD 要求的宿主目录/外来漂移/双宿主；S8 fakeAdapter 构造器缺位已按 DoD 预告标注（fixtures.ts:7 + 壳注记）。sha `b31187d` |
| **C3** 台账一致性脚本 | [x] | ✅ | `scripts/check-ledger.mjs` 存在可跑。实跑记录见下节。四类报告能力实证：host-matrix-alignment 跑报出「勾了没提交」类 4 项。sha `59e7834` |

## 三、C3 脚本实跑记录（审查席执行）

```
$ node scripts/check-ledger.mjs local-capability-plane
✓ local-capability-plane：7 勾 / 10 提交 / 7 台账 sha
台账一致    （exit 0）

$ node scripts/check-ledger.mjs local-capability-plane host-matrix-alignment
✓ local-capability-plane：7 勾 / 10 提交 / 7 台账 sha
✗ host-matrix-alignment：10 勾 / 8 提交 / 5 台账 sha
  [1 勾了没提交] 0.1 已勾但无 spec 尾注提交
  [1 勾了没提交] 0.2 已勾但无 spec 尾注提交
  [1 勾了没提交] 0.3 已勾但无 spec 尾注提交
  [1 勾了没提交] 0.4 已勾但无 spec 尾注提交
4 项不一致    （exit 1）
```

**席4 对 host-matrix 0.1–0.4 历史债的裁定建议**：C3 DoD 预告「首跑即暴露 host-matrix 0.1–0.4 历史无尾注勾选，留审查席裁定」。核查确认：该 4 项勾选属 local-capability-plane 立项**之前**的 host-matrix-alignment 历史产物，提交存在但 message 无 `spec:` 尾注，脚本按任务号主键无法关联——**非本线违规，不构成本线驳回理由**。建议汇总席转交 host-matrix 归属席补尾注或在 host-matrix 台账补条目，消除长期噪音。

## 四、progress.md ↔ git log 双向核对

progress.md 2026-09-12 共 7 条（`:17-23`），逐条核对：

| 台账 sha | 任务 | git log 命中 | 台账描述与产物一致 |
|---|---|---|---|
| `cd3dc12` | 0.1 | ✅ | ✅ |
| `b0d9bb0` | 0.2 | ✅ | ✅（含 CLI 5 新用例、protocol build 记载） |
| `7e83c1d` | 0.3 | ✅ | ✅ |
| `f76f666` | 5.1 | ✅ | ✅（含 verify-target.test.ts 7 用例、dist 重建记载） |
| `2653873` | 5.2 | ✅ | ✅ |
| `b31187d` | C1 | ✅ | ✅（16 壳 + fixtures 构造器清单与实存一致） |
| `59e7834` | C3 | ✅ | ✅（含「首跑暴露 host-matrix 债留审查席」记载，与本报告第三节呼应） |

双向结论：**7 勾任务 ↔ 7 台账条目 ↔ 7 个 sha 全部互认**；台账「✅ 待审查」状态与实际审查时点相符。零「勾了没提交 / 提交没勾 / 无台账条目 / sha 不存在」于本线。

## 五、总结论

### ✅ 通过（local-capability-plane 本线 7 项已勾任务 DoD 全过，附 3 项警告，无驳回项）

🟡 警告清单（均不阻塞，交汇总席）：

1. **🟡 host-matrix-alignment 0.1–0.4 历史债**（线外）：`check-ledger` 对其报 4 项「勾了没提交」（实为提交无 spec 尾注）。C3 DoD 已预告并留本席裁定，裁定见 §三——建议归属席补尾注/台账条目，避免 CI 化后长期红。
2. **🟡 progress.md「只追加不重写」纪律的执行口径**：C3 席「顺手修正两份台账 9 个 amend 废弃 sha」属就地改写既有条目（有 C3 新条目说明背书，动机正当）。建议汇总席在 tasks.md §0.7 补一句执行口径：**sha 失效类纠错允许就地修正 + 必须在新条目中声明**，否则后续席无所适从。
3. **🟡 `fec54b5`（dist 重建 + 夜间轮1工作记忆）无独立台账条目**：由 0.2 条目「protocol build」与 5.1 条目「dist 重建」文字覆盖，§0.7 实质（每会话有凭证）未破，但 chore 笔与台账粒度不一致。建议约定：dist/工作记忆合并笔计入同任务条目即可，无需独立条目。

**遗留提醒**（非警告，排期内）：0.4 快照测试（正/旧两版样例夹具）与 0.5 ★门 G-A 未动，属计划内未开工，next 轨道 A 应优先 0.4。
