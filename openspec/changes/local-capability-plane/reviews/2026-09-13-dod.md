# 审查报告·DoD 与台账一致性（席4·2026-09-13）

> 审查范围：2026-09-12 22:30 后落地的 `local-capability-plane` 与 `host-matrix-alignment` 两提案产出（后者 R5-a / 1.4 勘误实际落盘于 09-12 清晨窗口，属本次点名审查项，一并核验）。
> 审查方式：只读 + 实跑（`pnpm check` / `pnpm lint` / `packages/cli pnpm test` / 台账对账复算均由审查席当场执行，非转抄开发席留痕）。
> 对象提交（12 笔，git log --since '2026-09-12 22:30' 实测）：`7257f91`→`052920a`，明细见逐任务表。

## 一、逐任务核验表

### local-capability-plane

| 任务 | 勾选/注记 | 提交 | 产物证据（实核） | 判定 |
|---|---|---|---|---|
| **0.4** 快照测试 | `[x]` | `7257f91` | `packages/cli/test/artifact-snapshot.test.ts` 三用例齐：正样例 SKILL.md 字节级快照（frontmatter 首部逐字节断言）/ 私有键零泄漏（triggers/layers/domains/verify_target 逐一反断言 + origin 在内部形式）/ 旧样例（无首部 + `verify_target:null`）经 scan→loadDetail→verify 全链零破坏。实跑通过 | ✅ |
| **0.5** ★门 G-A（仅注记） | `[ ]` + 门请求注记 | — | tasks.md 保持未勾、零编码 ✓；progress.md 含完整抽验清单（5 步 + 通过标准 + 驳回路径）✓。符合 ★门「不编码，输出清单」协议 | ✅ |
| **1.1** `capability/types.ts` | `[x]` | `e3ace1f` | 文件 172 行，DoD 全量落地：两段接口（`PackageSource` / `CapabilityAdapter`）·`Cap` 九值冻结 ·`DeployResult.ladder` + `skipped[].reason` ·Availability/PackageRef/PackageMetadata/Package/PackageItem/RemoveResult/ReconcileReport（五态 `ReconcileState`）·`UnsupportedCapability`/`HostLockConflict` 结构化错（code/字段/自纠建议 message）。`capability-types.test.ts` 4 用例实跑通过；**`pnpm check`（tsc strict，root tsconfig 含 packages/*/src+test）实跑零错**——「类型编译过」独立复核成立 | ✅ |
| **1.2** `capability/caps.ts` | `[x]` | `10af524` | `FROZEN_CAPS` 九能力导出与 `Cap` 同源 ✓；`assertCap` 未声明抛 `UnsupportedCapability`（结构化字段 adapter/cap 齐）✓；`capability-caps.test.ts` 3 用例含**全九能力逐一拒绝**断言，实跑通过；C2 `options.gate` 消费位已就绪（contract.ts L114-116） | ✅ |
| **5.3** 三计数分立 | `[x]` | `e9dd1cf`+`5162dd2` | ① schema：`retrieved`/`injected` 两新计数 + default 工厂化 ✓；② `lifecycle.bumpSkillMetrics`（批量、tmp+rename 原子写）✓；③ 双埋点：`skillTools.ts` search 命中→retrieved、`loader.ts` loadDetail 成功→injected ✓；④ `used=use_count` 语义零改名 ✓；⑤ 连带 `verify.ts` 写回加 `...m` 展开保新字段（`5162dd2`）✓；⑥ verify-target.test.ts +52 行三路径断言。实跑全绿 | ✅ |
| **zod default 全仓整改**（5.3 连带闭环） | `[x]`（记于 5.3 行） | `241ba69` | `skill-schema.ts` 六处对象字面量 default 全部工厂化（L82 dependencies / L132 lifecycle / L180 experience / L279 domains / L288 compression / L290 sync，grep 实核）；`schema-defaults.test.ts` 三组引用隔离防回归。**审查席独立验证 zod@4.4.3 语义：对象字面量 default 确跨 parse 共享可变引用（bug 为真），工厂隔离；数组 `.default([])` 在 zod v4 已值拷贝隔离——故整改范围（6 处对象字面量）完备，无遗漏面** | ✅ |
| **C2** 契约套件 | `[x]` | `21e71d2` | `scenarios/contract.ts`：probe 结构（缺席必带 reason）/capabilities 非空/hosts 非空/deploy ladder∈L1-L3 + skipped 逐条 reason/reconcile 硬要求 + 五态校验/声明一致性（未声明 remove 必抛 UnsupportedCapability）/gate+lock 钩子位 ✓；`contract-adapter.test.ts` fakeFull+fakeNoRemove 四自证（含缺席 reason 拒绝、reconcile 违约拒绝）实跑通过；「3 行接入」示范成立 | ✅ |

### host-matrix-alignment

| 任务 | 勾选 | 提交 | 产物证据（实核） | 判定 |
|---|---|---|---|---|
| **1.2** `wiring/hostSkills.ts` Hermes skills 落盘 | `[x]` | `29b5f3d` | `packages/cli/src/skills/wiring/hostSkills.ts`：`writeHostSkill` 复用 `renderArtifactSkillMd`（宿主侧仅 SKILL.md，内部形式零泄漏）·`SKILL_ID_PATTERN` 白名单 fail-fast 防穿越 ·tmp+rename 原子写；`removeHostSkill` 只删自己条目（无 SKILL.md 视为 foreign 不动）、零残留。`wiring-hostskill.test.ts` 5 用例（首部合规/幂等覆盖/五种非法 id 拒绝且零越界目录/卸载对等/非法 id fail-fast）实跑通过。注：init 全链接线按任务文本留 1.6/1.9，src 内暂无消费方——与勾选注记一致，非账实不符 | ✅ |
| **R5-a** removeYamlHook 逐条删除 | （审查整改项） | `dda2731` | `hooksYaml.ts removeYamlHook` 重写为逐条删除：块内仅滤 `command: "<我方>"` 行、用户同块自有 command 原样保留、块删空才清 event 键；`wiring-hermeshook.test.ts` 新增 R5-a 红测（user-own-hook 毫发无损断言）实跑通过 | ✅ |
| **1.4** 台账勘误 | （勘误笔） | `cbb3cdf`+`179baa5` | progress.md 1.4 行 sha 定稿 `b905829`、R5-a 行 `dda2731`，均为 git 可达终值；台账↔git log 双向可对 ✓ | ✅ |

## 二、全量门禁实跑（审查席复跑，非转抄）

| 门禁 | 结果 |
|---|---|
| `pnpm check`（tsc strict，覆盖 packages/cli src+test） | ✅ 零错误 |
| `pnpm lint`（biome，140 files） | ✅ 零告警 |
| `packages/cli pnpm test` | ✅ **163 pass / 0 fail / 16 todo**（todo=S1–S16 场景壳，C1 预期）——与台账最新留痕「CLI 163/163」一致 |
| Docker 全链 verify / e2e | ⏸ 仍按既留痕替代口径（Docker 阻塞为多日既存状态，非本夜新增缺口；账面已如实留痕） |

## 三、台账一致性（check-ledger + sha 链抽查）

### 3.1 台账对账实质状态（手工复算，逻辑与 C3 脚本逐条同构）

- **local-capability-plane**：12 勾 / 21 提交——**一~三类违规 0**；四类 sha 失效 warning **2 条**：`ce424aa`、`201baeb`（见 3.3）
- **host-matrix-alignment**：11 勾 / 12 提交——仅 **0.1–0.4「勾了没提交」历史债**（调研四笔落在 docs/ 路径、早于 spec 尾注约定，与预期完全一致，留审查席裁定=维持现状不回勾）

### 3.2 🔴 C3 交付物自身缺陷：`scripts/check-ledger.mjs` 遇死 sha 即 crash

实跑 `node scripts/check-ledger.mjs local-capability-plane host-matrix-alignment` 抛
`TypeError: warnings.push is not a function (scripts/check-ledger.mjs:121)`。
根因：L89 `let warnings = 0`（数字计数器）与 L97 `changeWarnings`（数组）混用，L121 误向计数器 push。该 bug 自交付笔 `59e7834` 即存在——**四类报告的第 4 类（sha 失效 warning）从未被真实执行过**，首遇死 sha 必崩。C3 DoD「能报出四类」未完整交付（前三类经手工复算验证正确）。一行修复（`warnings.push`→`changeWarnings.push`），建议汇总席当晚修并补一条死 sha 夹具测试。

### 3.3 🟡 §0.7 sha 勘误口径执行不完整（progress.md 抽查：最近 5 条 2 条失配）

LCP progress.md 末 5–6 条 sha 链实核：

| 台账行 | 记载 sha | git 实况 | 判定 |
|---|---|---|---|
| 0.4 快照测试 | `e3ace1f` | 实际产物笔为 `7257f91`；`e3ace1f` 可达但是 1.1 的提交 | 🟡 误归属（「sha见下笔」回填写错对象） |
| 1.1 types | `e3ace1f` | 可达、正确 | ✅ |
| 5.3 三计数 | `e9dd1cf` | 可达、正确（有 `13b2e46` 定稿笔） | ✅ |
| 02:00 轮 zod 整改 | `ce424aa` | **不可达**（amend 前值，真身 `241ba69`） | 🟡 死 sha，无定稿笔 |
| C2 契约套件 | `201baeb` | **不可达**（amend 前值，真身 `21e71d2`） | 🟡 死 sha，无定稿笔 |
| 1.2 能力门控 | `10af524` | 可达、正确（有 `052920a` 定稿笔） | ✅ |

同夜的 5.3/1.2/host-matrix 1.2 三行都有「docs·sha 定稿」勘误笔，唯 zod 整改与 C2 两行漏做——且这 2 个死 sha 正是 3.2 crash 的触发器（修复脚本前，工具对 LCP 永远跑不出干净报告）。host-matrix 台账末 6 条 sha（`e7fc8f2`/`480c128`/`1ed81f3`/`b905829`/`dda2731`/`29b5f3d`）全部可达且正确，勘误纪律执行到位。

## 四、总结论

**🟡 警告通过（不驳回任何任务）**

- **代码面全绿**：十项受审任务产物全部真实存在、行为与 DoD 逐条相符、测试为真实断言且审查席独立复跑通过（check/lint/CLI 163+16todo 全绿）；zod 整改范围经独立实验证实完备；R5-a 与 1.4 勘误整改到位。
- **短板集中在「对账基建自身」**：① C3 脚本第 4 类报告路径带病交付、遇真实死 sha 即 crash（🔴 级工具缺陷，影响面=审计能力本身，一行可修）；② LCP 台账 2 条死 sha 未按 §0.7 勘误口径定稿 + 1 条 sha 误归属（🟡，与 ① 互为因果）。
- **移交汇总席三件事**：修 `check-ledger.mjs` L121（`warnings.push`→`changeWarnings.push`）+ 死 sha 夹具；progress.md 两行 sha 勘误（`ce424aa`→`241ba69`、`201baeb`→`21e71d2`）与 0.4 行勘误（→`7257f91`）；host-matrix 0.1–0.4 历史债裁定（建议：维持现状，属约定前产物，C3 行注记已留痕）。
- **留痕核对**：0.4 的 Docker-verify 替代口径、0.5 门请求注记、G-A 抽验清单均如实落账，无虚勾；未发现「勾了没提交」「提交没勾」新增实例。
