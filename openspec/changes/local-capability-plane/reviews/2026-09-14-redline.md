# 审查报告·红线七条（席6·2026-09-14）

- **审查范围**：2026-09-13 22:30 后全部改动，共 14 笔提交——local-capability-plane 1.3（278029f）/ 1.4（a072a0a）/ 1.5（c539470）/ 5.4（462ea46）/ C6（5833b7e）+ host-matrix-alignment 1.6（c0ba0cd）/ 1.7（9771e13）+ 各随行 docs sha 定稿 ×7。**口径勘误**：任务书所列 0.4 / 1.1–1.2 / 5.3 / C2 / zod 整改与 host-matrix 1.2 / 1.4 均落在 22:30 之前（上夜已审，见 [2026-09-13-redline.md](2026-09-13-redline.md)），未入本窗；窗口内 feat 提交实为上述 7 笔。
- **权威条文**：[proposal.md §2.1 红线七条](../proposal.md)；host-matrix [tasks.md](../../host-matrix-alignment/tasks.md) 头部纪律行（测试随行 · 账实同步 · 只碰自己条目 E-10 · **协议零变化** · **零触碰 apps/api**）；审查清单 [templates.md §四](../templates.md)；上夜前瞻项 R5-a（已闭环）/ R5-b（限期 1.7 接线前收敛）见 [2026-09-13-redline.md](2026-09-13-redline.md)。
- **审查方式**：只读，零 git 写操作。逐笔 `git log --name-status` + 新增源文件全读（ledger/lock/broker/summarize-reviews/context）+ E-10 重点文件现状全读（hooksYaml.ts / hostSkills.ts / snippets.ts / uninstall.ts）+ 窗口 7 个测试文件夹具逐核 + 实跑（窗口 7 文件 **40/40**；CLI 全量 **182 pass / 0 fail / 16 todo**，todo 为既有场景壳）。

## 红线逐条

| # | 红线 | 结论 | 证据 |
|---|---|---|---|
| 1 | 九工具面不变；维护类能力不开 MCP 工具、不设用户命令 | ✅ | 窗口 src 改动：capability/{ledger,lock,broker}.ts（新库，纯类型+纯函数+进程内锁）、context.ts（推通道内部实现，文件头自证「不进 USAGE 不进命令面」，仅加 --event 解析）、status.ts（既有 status 命令体内加一行呈现 + 纯聚合函数，零新增命令）、wiring/snippets.ts（接线内部）。零 MCP 工具/用户命令新增；participant SKILL.md 未动 |
| 2 | 无常驻进程 | ✅ | 新代码正则扫描零 `setInterval/setTimeout/spawn/listen/cron`；lock.ts 为进程内 Map（注释自证 CLI 短命令生命周期，跨进程归 P2）；ledger tmp+rename 原子写；summarize-reviews.mjs 为按需单次脚本非守护进程 |
| 3 | 零触碰 `apps/api` | ✅ | 窗口 14 笔提交文件清单无一含 `apps/api`；openapi.json 未动 |
| 4 | Zero-LLM | ✅ | 零模型调用、零网络请求（唯一 `fetch` 命中为 capability/types.ts 接口方法声明，非窗口改动、无实现） |
| 5 | 写只碰自己的条目（E-10）；读可全量 | 🟡（写入面合规；**摘除面断链**，见重点复核 A） | hostSkills.ts：id 白名单 SKILL_ID_PATTERN=`/^[a-z][a-z0-9_-]*$/`（无斜杠无点，path.join 不可能穿越），卸载仅删含 SKILL.md 的自己目录（foreign 不动）；hooksYaml 注入/摘除均为行级最小操作，removeYamlHook 逐条删除（R5-a 成果未回退）；ledger 只写 `~/.agentsignal/ledger.json5`；1.7 兜底降级异常落 stderr 可见。**但 1.7 把 injectHermesHook 接入生产路径后，removeHermesHook 仍零生产消费方 → Hermes 卸载残留 config.yaml hook + allowlist 条目** |
| 6 | 不引入微服务 / K8s / 消息队列 | ✅ | 窗口零 package.json 变更、零新依赖；C6 脚本零外部请求 |
| 7 | CLI 命令面变更同 PR 同步 participant SKILL | ✅（N/A） | 零命令面变更，SKILL.md 正确未动，G1–G4 不触发 |

## 重点复核 A：Hermes hook 摘除链断（当夜最重要发现 → 整改项 R6-a）

**事实链**（全仓 grep 坐实）：

1. 1.7（9771e13）`writeExtra` hermes 分支首次生产接入 `injectHermesHook`（snippets.ts:150，写 `config.yaml` hooks 条目 + `shell-hooks-allowlist.json` 双写）；
2. `removeHermesHook`（hooksYaml.ts:130，功能完整、单测覆盖）**全仓仅测试消费，零生产调用方**；
3. 卸载链 `uninstallAll → unwireMcp → removeExtra` 的 hermes/soulPath 分支（snippets.ts:231–241）只调 `removeSoulBlock`。

**后果**：init 在 HERMES_HOME 落三件产物（wiring-hermes.test.ts 已断言 SOUL.md + config.yaml + allowlist 三件），uninstall 只摘 SOUL.md——config.yaml `hooks.SessionStart` 条目与 allowlist 条目**残留**，且残留 hook 会在 Hermes 每次会话开工时继续执行 `agentsignal context`（静默退出契约兜底故无害，但宿主侧挂着指向已卸载产品的 hook 注册）。tasks.md 1.2 勾选项「卸载只删自己条目零残留」在 hook 面不成立——非红线第 5 条「写越界」（写没碰别人），而是**自己条目摘不干净**，E-10 卸载对称面破损。

**整改要求（R6-a，限 1.9 真机走查前）**：`removeExtra` soulPath 分支补 `removeHermesHook(host.hooksYamlPath, host.hookAllowlistPath, "SessionStart", CMD)`（hosts.ts 字段已备）+ 卸载用例断言双文件归零。修复成本 ≈ 一行 + 一条断言。

## 重点复核 B：夹具纪律 + 实跑取证

| 测试文件 | 夹具 | 判定 |
|---|---|---|
| capability-ledger | `mkdtemp(tmpdir())` + `AGENTSIGNAL_CONFIG` | ✅ |
| capability-lock / capability-broker | 纯内存零 IO（lock 进程内 Map / broker fake 适配器） | ✅ |
| context-event | `mkdtemp` + `process.chdir` 未初始化夹具（Stop 早退零 FS） | ✅ |
| verify-target | `mkdtemp` + `AGENTSIGNAL_CONFIG` | ✅ |
| wiring-hermes / wiring-hermeshook | `mkdtemp` + `AGENTSIGNAL_HOME` + `HERMES_HOME` | ✅ |

零用例触碰真实宿主配置；实跑窗口 7 文件 40/40、CLI 全量 182/0/16，全绿。

## 附加判据

| 检查项 | 结论 | 证据 |
|---|---|---|
| 协议零变化（REST/信封） | ✅ | packages/protocol 窗口内**零文件改动**（任务书所提 skill-schema.ts zod 整改属上夜窗口，本窗 protocol 无提交） |
| 零 LLM / 零定时器 / 零常驻 | ✅ | 见红线 2/4 |
| 勾选一致 | ✅（附 R6-b 宣称偏差） | 7 笔 feat ↔ tasks.md [x] 一一对应（1.3/1.4/1.5/5.4/C6/1.6/1.7）；docs 均为 sha 定稿。**例外**：1.7 宣称「R5-b 空行压缩越界顺手收敛同域」，但 hooksYaml.ts 自 dda2731（上夜）后零改动，`removeYamlHook` 末尾 `.replace(/\n{3,}/g, "\n\n")` 仍**全文件**生效——宣称与代码不符（🟡-2） |
| 降级可见 | ✅ | 1.7 兜底 `console.error` 落 stderr，wiring-hermeshook.test.ts「1.7 兜底」用例断言 stderr 含 Hermes 告警 + extra=none + 不 fail |
| 工作区 | ✅ | `git diff HEAD --stat` 为空；`git status` 仅 `?? reviews/2026-09-14-test.md`（当夜测试席报告，并行审查设计内产物，汇总席统一提交） |

## 🟡 整改 / 备案项（不构成红线违反，按序处置）

1. **R6-a（限 1.9 前，见重点复核 A）**：removeExtra hermes 分支补 removeHermesHook 调用 + 卸载双文件归零断言；否则「卸载零残留」验收语义不成立。
2. **R6-b（随 R6-a 同 PR）**：R5-b 宣称与代码不符——二选一：真收敛（`.replace(/\n{3,}/…)` 限缩到摘除块内作用域）或台账/progress 改口「保留现状 · 低危豁免」，不得留「已收敛」假账（账实同步纪律 §10）。
3. **R6-c（1.6 功能缺陷，建议随 R6-a 修）**：`--event SessionStart`（空格形态）的值漏进 query——context.ts:15 `args.filter(!startsWith("--"))` 只剔 flag 不剔值，production hook 命令（snippets.ts 写入的 `agentsignal context --event SessionStart`）实际以 query="SessionStart" 运行：既非「SessionStart→全量推」语义，又压掉 `AGENTSIGNAL_HOOK_INPUT` 兜底（L16 `query || env`）。修法：parseEventArg 解析后把事件值从 args 中剔除。`--event=` 等号形态不受影响。
4. **备案（低危，非本窗引入）**：hooksYaml 行级注入的假阳性面——内联 `hooks: {}`、子键下缩进 `hooks:`、块标量内 "hooks:" 行会误定位（注入仍只写我方行，但可能落错父键产生重复 hooks 键）。1.4 既有已知局限，P2 对账器落地时加解析校验一并兜住。另：C6 脚本落位 `scripts/summarize-reviews.mjs` 与 tasks.md C6 指名一致（合规）；AGENTS.md「运维脚本（scripts/ 仅保留备份还原两件）」表述已过时，随下次 AGENTS.md 刷新一并更新（文档漂移备案）。

## 总结论

**🟡 警告通过**——红线七条当夜零违反：协议零变化、零触碰 apps/api、Zero-LLM、无常驻/定时器、零新依赖、命令面零变化全部坐实（CLI 全量 182/182 实跑绿，窗口 40/40）；E-10 写入面（白名单防穿越 + 行级注入 + R5-a 逐条删除）合规。但 **1.7 生产接入 hook 注入而摘除链未接（R6-a，卸载残留）+ R5-b 宣称与代码不符（R6-b）+ 1.6 事件值污染 query（R6-c）** 三项需限期整改，R6-a/R6-b 限 1.9 真机走查前，R6-c 建议同 PR。不阻塞已落盘功能的当夜验收，阻塞 Hermes 全环验收。

—— 审查席 6（红线）· 2026-09-14 · 只读审查，零 git 写操作
