# 审查报告·DoD 与台账一致性（席4·2026-09-15）

> 审查范围：2026-09-14 22:30 后两提案产出（`git log --since` 实测 **14 笔 = 7 feat + 7 docs 成对**：LCP 1.6/5.5/4.1/4.2/C4/C5（C3 修复笔 `54bd4f6` 落于 09-14 08:02 窗口外，本席做效果复验）· HMA 1.8）。
> 审查方式：只读 + 实跑——窗口内五个测试文件 19 用例当场实跑、`check-ledger.mjs` 双库实跑、C4 bench 实跑出数、C5 清单抽两条 grep 实证。
> `git status` 审前干净；本报告按协议留汇总席统一提交。

## 一、逐任务核验表

### local-capability-plane（窗口内 6 项 + C3 复验）

| 任务 | 勾选 | 提交 | 产物证据（实核） | 判定 |
|---|---|---|---|---|
| **1.6**（实为 1.7 DoD，见 🟡-1）降级矩阵首条夹具 | `[ ]`（1.6 与 1.7 均未勾） | `d3966d7` | `contract-degrade.test.ts` 3 用例实跑绿：矩阵首行三档（claude-code→L1 written 断言 / cursor→L2+reason 可见 / gemini→L3+reason 可见，D10 不静默）；降级 fake 过 C2 `runAdapterContract` 全套；S6 形态 native `probe:{available:false}` → broker `selectAdapter` 降级接管不报错。**产物真实且绿，但任务号错挂**（tasks.md 1.6=apm.ts 适配器，产物不存在；降级矩阵夹具属 1.7 条款） | 🟡 |
| **5.5** S2/S3 本地半场景入库 | `[x]` | `a1eef5d` | s02：三路径分别 bump/loadDetail/verify 后 `retrieved=2·injected=1·use_count=1` 互不串扰断言实跑绿；s03：三态落 `metrics.partial` + `verify_target:null` 归一 + `provenance.sig_id`/`origin=platform` 双溯源断言实跑绿；两壳确由 `test.todo` 填充为真实断言（git diff 实证），**全仓 scenario todo 16→14 属实**（grep 计 14） | ✅ |
| **4.1** apm.yml 导出器 | `[x]` | `9934dee` | `export.ts exportApm`：产物复用 `renderArtifactSkillMd`（tmp+rename 原子写）·apm.yml 标准段零私有键·两次导出字节级一致断言（确定性）·未知 id 尽力而为跳过不阻塞·空包 `skills: []` 结构——4 用例与 DoD 逐条对应，实跑绿；S10 泄漏面（triggers/layers/domains/keywords/provenance/base_url）逐一负断言 | ✅ |
| **4.2** apm.yml 导入器 | `[x]` | `bc8daa1` | `importApm`：行级解析自家 4.1 输出契约·`parseArtifactFrontmatter` 首部校验（name=目录名）·`origin={kind:"apm-import",ref:yml 包名}` 断言过·私有层安全默认重建（domains common/layers base/triggers keyword=包名，**不从产物回转**）断言过·S10 往返正文逐字无损断言过。**唯「坏条目结构化跳过」零用例、实落 2 用例 ≠ DoD 记 3 用例**（见 🟡-2） | 🟡 |
| **C4** 性能基线 | `[x]` | `fdb9113` | `scripts/bench/bench-cli.mjs` 薄入口（spawn `--experimental-strip-types` 解析 cli 包内 bench.ts，模块解析按文件位置的注记属实）+ bench.ts：检索（scan/建库/100 次均值·中位·P95）/装配（loadDetail 单次）实跑出数（20 条档：检索均值 0.06ms·装配 7.9ms，与 DoD 记载量级一致）·对账路径显式占位待 P2 2.3；不入 CI 门禁符合 DoD | ✅ |
| **C5** 手册随行检查 | `[x]` | `1de027d` | `reviews/c5-manual-consistency.md` 在库；15 命令矩阵 + 零 P0 不一致结论 + 4 项「待 0.6.0 发版随行」预留，符合 §11「手册只写已上线能力」。**抽查两条均通过**：① `context` 在 user-manual/participant SKILL/admin-guide 三册 grep **零命中**（内部命令豁免属实）；② `me / ls / edit / rm` 确在 user-manual 有记载（第 20 行通道表）——唯定位符「✓(§2)」小偏（§2 起于 58 行），内容无不一致 | ✅ |
| **C3** 跨库尾注解析（窗外修复·效果复验） | `[x]` | `54bd4f6`（09-14 08:02） | 实跑 check-ledger：`4f1f044` 尾注「1.6」**不再误挂 LCP**（正确归属 hma 1.6 已勾项，零噪声）；progress.md:58 豁免注记在档——修复生效 | ✅ |

### host-matrix-alignment（窗口内 1 项）

| 任务 | 勾选 | 提交 | 产物证据（实核） | 判定 |
|---|---|---|---|---|
| **1.8** 测试收口 | `[x]` | `d4c6d41` | 新增「探测命中：HERMES_HOME 目录存在 → detectHosts 报 hermes」用例真实存在且绿（`mkdir .hermes` → `detected=true` + `mcpPath=null` D1/D7 断言）；勾选行注记「三断言+专项 3 条由 1.1–1.7 四文件 19 用例分摊、本笔仅补探测命中」与实测相符（wiring-hermes 单文件恰 8 用例全绿）；Phase 1 夜间面仅剩 1.9 真机（站长项） | ✅ |

## 二、发现与警告

**🔴 驳回项：无。**

**🟡-1（LCP「1.6」任务号错挂，账实需勘误定稿）**：`d3966d7`+`995f556` 提交与台账均记「P1 1.6 契约落地」，但 tasks.md **1.6 = `capability/adapters/apm.ts`**（未勾，产物 `src/capability/adapters/` 目录不存在）；「降级矩阵第一条夹具」实为 **1.7 条款**（1.7 亦未勾）。check-ledger 实跑因此新报 `[2 提交没勾] 1.6 有提交但 tasks.md 未勾`（= feat+docs 成对两笔）。progress.md:58 昨夜注记「LCP 1.6（契约接入）仍未实施」的表述同样与 tasks.md 1.6 定义不符——编号混淆已持续两夜。产物本身真实、19 用例全绿，**非质量驳回，属台账勘误**。建议汇总席：① 把两笔提交/台账/测试文件头注（contract-degrade.test.ts:2 亦写「P1 1.6」）勘误归属 **1.7**；② 核 1.7 DoD（「契约套件入库，verify 全绿」——C2 套件已入库、本笔降级 fake 过全套）后勾选 1.7；③ 1.6（apm.ts 适配器）保持 `[ ]` 待认领。

**🟡-2（4.2 用例数未达 DoD 记载）**：tasks.md 4.2 记「**3 用例**含往返无损」且条款明列「**坏条目结构化跳过**」，实落仅 2 用例（导入溯源 + S10 往返）；`importApm` 的 catch→`skipped` 分支（export.ts:139-144）**零测试覆盖**——首部缺失/坏 YAML 路径目前只能靠人工触发。建议汇总席令 4.2 回 `[~]` 或下夜补 1 用例（构造无首部产物文件 → 断言 `skipped[].reason` 结构化返回、其余照常导入）后回 `[x]`，实现本身无须改动。

**ℹ️ 备忘（不阻塞）**：

1. s03 的 verify_target 关联仅在 **null 归一分支**断言（样例无 verify_target）；正向关联（statement/checks 归一 + 判定依据）已由 5.1 `verify-target.test.ts` 单测覆盖，场景层可接受，S3 平台半聚合（V3）时再补全链。
2. C5 矩阵「me/ls/edit/rm ✓(§2)」定位符小偏（实际在 user-manual 首部通道表，第 20 行）；纯定位精度，命令面覆盖事实成立，发版随行时顺手修正即可。

## 三、台账与 sha 链（check-ledger 实跑）

- `node scripts/check-ledger.mjs local-capability-plane host-matrix-alignment` 实跑（exit 1，共 5 项）：
  - LCP `22 勾 / 51 提交 / 27 台账 sha`，1 项 = 上述 🟡-1「1.6 有提交但 tasks.md 未勾」（本夜新产生，非历史债）；
  - HMA `14 勾 / 19 提交 / 11 台账 sha`，4 项 = **0.1–0.4 历史无尾注勾选**（C3 DoD 预登记已知债，维持前夜裁定：登记豁免或一笔覆盖笔补账，不回填）；
  - **预期残留达成**：hma 0.1–0.4 之外仅 🟡-1 一项新增；`4f1f044` 跨库误挂经 C3 修复后零噪声（progress.md:58 豁免注记在档）。
- 7 笔 docs「sha 定稿」成对笔抽查（995f556/2f2c9c4/98b0f76/cd9c421 等）均把对应 feat sha 写进台账，与 git log 一一对上；7 个 feat 提交在 progress.md（LCP 6 条 + HMA 1 条）全部有「✅ 待审查」条目，无「有提交无台账」。

## 四、全量门禁实跑（审查席复跑，非转抄）

| 门禁 | 结果 |
|---|---|
| 窗口内五测试文件（contract-degrade/s02/s03/capability-export/wiring-hermes） | ✅ **19 pass / 0 fail**（约 240ms） |
| `check-ledger.mjs` 双库 | ✅ 可跑通（exit 1 = 已知历史债 4 项 + 🟡-1 新项，见 §三） |
| `scripts/bench/bench-cli.mjs 20` | ✅ exit 0，数字与 DoD 量级一致（检索 0.06ms·装配 7.9ms·对账占位） |
| scenario todo 计数 | ✅ 14（16→14 属实） |
| Docker 全链 verify / e2e | ⏸ 按既留痕替代口径（多日既存状态，非本夜新增缺口） |
| `git status` | ✅ 审前干净（本报告 untracked 留汇总席统一提交） |

## 五、总结论

**🟡 警告通过（零驳回）**——窗口内 8 项产出（LCP 6 + HMA 1 + C3 复验）产物全部真实、19 用例实跑绿、台账/sha/脚本三方对账除任务号错挂外全通。两项 🟡 移交汇总席定稿：**1.6↔1.7 任务号勘误（含提交尾注/文件头注/台账三处）+ 勾选 1.7**；**4.2 补坏条目跳过 1 用例**。均低成本，不阻塞今夜定稿。
