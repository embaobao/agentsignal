# 审查报告·DoD 与台账一致性（席4·2026-09-14）

> 审查范围：2026-09-13 22:30 后两提案产出（`git log --since` 实测 **14 笔 = 7 feat + 7 docs 成对**：LCP 1.3/1.4/1.5/5.4/C6 · HMA 1.6/1.7）。
> 审查方式：只读 + 实跑——窗口内六个测试文件 57 用例当场实跑、check-ledger / summarize-reviews 两脚本当场实跑、zod default 语义 node 实证、17 个台账 sha 逐一 `cat-file`+`merge-base --is-ancestor` 核可达。
> 范围注记：审查清单所列 0.4/1.1/1.2/5.3/zod 整改/C2（LCP）与 1.2/1.4（HMA）物理落盘于 09-12/13 夜（窗外），前夜报告已逐条审过；本席做「产物仍在 HEAD + 零 Shim 残留」复验（§四），不重开逐条复审。

## 一、逐任务核验表

### local-capability-plane（窗口内 5 项）

| 任务 | 勾选 | 提交 | 产物证据（实核） | 判定 |
|---|---|---|---|---|
| **1.3** `capability/ledger.ts` 归属账本 | `[x]` | `278029f` | schema v1（`agentsignal.ledger/v1`）·tmp+rename 原子写（`.tmp-<pid>`，测试含 tmp 残留零断言）·`writeQueue` promise 链互斥（**并发 10 append 零丢写实跑过**）·`snapshotOf` sha256 与 `node:crypto` 对表断言 ·loadLedger 缺失/损坏 catch 兜底空账本 ·entries 只追加 ·root 单一真源 ·跨进程互斥升级注记在档——DoD 逐条兑现，**唯「损坏读入」半边无用例**（见 🟡-2） | ✅ |
| **1.4** `capability/lock.ts` 归属锁 | `[x]` | `a072a0a` | host×cap 槽位 Map 互斥 ·`HostLockConflict` 含 holder+host+unwire 建议 message ·冲突/释放/复得（冲突方指向新持锁者）/双重释放幂等/异槽不串 五组断言实跑过——DoD「冲突/释放/复得三断言」成立。注：`lockHolder` 导出零测试零消费（见 ℹ️） | ✅ |
| **1.5** `capability/broker.ts` 选择器 | `[x]` | `c539470` | preferred 置顶（稳定排序保传入序）→ native 加权（rank 0）→ probe 不可用逐个换不报错 → 全不可用 undefined 走降级 D10；六断言（直选/缺席接管 S6/native 压序/逐换/全缺/空列表）fake 双适配器实跑过，纯逻辑零副作用 | ✅ |
| **5.4** status 验证体检 | `[x]` | `462ea46` | `verificationReport` 纯函数（三态 worked/partial/failed ·三计数 retrieved/injected/used=use_count 语义零改名 ·上游 traced/total+base_url 站点去重）+ statusCmd「验证体检」行，**零新增命令**；「输出行断言 + 纯函数字段断言」双覆盖实跑过（verify-target.test.ts） | ✅ |
| **C6** summarize-reviews.mjs | `[x]` | `5833b7e` | 实跑两 change：LCP 聚合三席最新报告（总结论行 + 🔴/🟡 项逐条，与现存 6 份报告内容对得上）；HMA 无 reviews 目录优雅空输出；**退出码恒 0、裁决权留汇总席**——「审查席一次跑通」成立 | ✅ |

### host-matrix-alignment（窗口内 2 项）

| 任务 | 勾选 | 提交 | 产物证据（实核） | 判定 |
|---|---|---|---|---|
| **1.6** context.ts 事件映射 | `[x]` | `c0ba0cd` | `parseEventArg`（`--event X` / `--event=X` / 其余参数留 query）+ `mapHookEvent`（SessionStart/未知→push·**Stop→noop 静默早退不硬造收尾**，早退在 createEngine 之前）；parse/映射/未初始化 Stop 零输出三测实跑过 | ✅ |
| **1.7** 兜底守卫 | `[x]` | `9771e13` | 核心 DoD 齐：writeExtra hermes 分支整体 try/catch（**EISDIR 占位红测：wireMcp 不 fail、extra=none、stderr 可见**）+ hook 命令带 `--event SessionStart`（1.6 消费）+ wiring-hermes 零垃圾断言升级固定三件产物（SOUL.md/config.yaml/allowlist，无 MCP 文件）实跑过。**但勾选行含未兑现子句「R5-b 顺手收敛」**（见 🟡-1） | 🟡 |

## 二、发现与警告

**🔴 驳回项：无。**

**🟡-1（hma 1.7 勾选含未兑现子句）**：tasks.md 1.7 行尾写「R5-b 空行压缩越界顺手收敛同域」，但 `hooksYaml.ts:100` `removeYamlHook` 的 `/\n{3,}/g` 全文件空行压缩自 `b905829`（1.4，09-12）引入后从未改动（`git log -S` 实证），`9771e13`（1.7）提交未触碰该文件——09-13 红线 🟡-2 的处置指引恰是「1.7 接线前顺手收敛」。残留本身低危（仅空白整理），但**勾选项声明 ≠ 事实**，按 §10 账实口径须处置。建议：汇总席把 1.7 行注记改「R5-b 未收敛、顺延下夜」或下夜轨道顺手清（一行改动：收敛为块内整理或原样 join），并对「顺手收敛/顺手清」类措辞加自查强度。

**🟡-2（lcp 1.3 DoD 半边未测）**：DoD 明写「**损坏**/缺失读入 v1 空账本零阻断」，实现 `loadLedger` catch 兜底在（ledger.ts:54-64），但 capability-ledger.test.ts 只有缺失（空账本）用例，无损坏（非法 JSON5 内容）用例。补一测即闭环，建议下夜 C 轨或修复席带走。

**ℹ️ 备忘（不阻塞，登记防失忆）**：

1. `lockHolder`（lock.ts:38）零测试零消费——DoD 注「供 status 呈现与对账」属 P2 2.6 接线位，接线落地时自然覆盖即可。
2. 5.3「全仓 6 处…已全部函数工厂化」清扫声明不完全：现存另两处字面量 default——`skill-schema.ts:277` 内层 `available: …default(["common"])`（外层 domains 已有工厂护体，防回归测试只测外层路径）与 `apps/api/src/routes/agents.ts:17` `RegisterRequestSchema.default({})`（两提案文件域之外，「零触碰 apps/api」红线成立，转登记）。本席 node 实证（zod 4.4.3）：这两处形态（扁平字符串数组/空对象）均实例隔离、无实证污染；**但同测试证实字面量含嵌套对象/数组时污染仍会传播**（5.3 原 bug 形态复现为真，09-13 dod 报「bug 为真」与本席实测一致）——工厂化仍是必要约定。建议：两处仅作约定尾差登记不抢修；schema-defaults.test.ts 头注「全 schema 必须引用隔离」措辞建议降为「点检 schema」或扩成扫描式（二选一，防「全」字虚标）。
3. hma progress.md 1.7 条「Shim 遗留顺手清」归属笔误：Shim 实际由 LCP 09-13 修复轮 `03f474f` 清（本席 grep 全仓零 Shim 残留），非本笔产出。纯台账措辞偏差。

## 三、台账与 sha 链（check-ledger 实跑 + 抽查）

- `node scripts/check-ledger.mjs local-capability-plane host-matrix-alignment` 实跑：**C3 crash 修复后 warning 路径正常工作**（不再中断）。输出：`✓ local-capability-plane：17 勾 / 35 提交 / 20 台账 sha`（零差异）；`✗ host-matrix-alignment：13 勾 / 16 提交 / 9 台账 sha`，4 项不一致全部是 **0.1–0.4「勾了没提交」**——C3 DoD 预登记的已知历史债（调研/文档四笔早于台账纪律建立，2026-09-03 完成）。**本席裁定**：历史段事实成立，建议登记「豁免（历史段）」或一笔覆盖笔补账，不回填。
- progress.md 抽查：两台账各取最近 6 条（LCP：汇总笔「(sha见git log)」指针形态 + 1.3/1.4/1.5/5.4/C6；HMA：1.4/2.4+2.5/R5-a/1.2/1.6/1.7）连同上夜三条勘误定稿笔（0.4→`7257f91`·zod→`241ba69`·C2→`21e71d2`），**17 sha 全部 REACHABLE 且为 HEAD 祖先**；7 笔 docs sha 定稿均成对把 feat sha 写进台账（抽查 `41c1ef5`/`8ead2fe` diff 内容一致）。勘误口径（§0.7 豁免）执行良好。

## 四、窗外在册产物复验（抽查）

`artifact-snapshot.test.ts`（0.4）·`capability/types.ts`（1.1）·`caps.ts` FROZEN_CAPS+assertCap（1.2）·`lifecycle.bumpSkillMetrics`+zod 整改（5.3）·`scenarios/contract.ts`+`contract-adapter.test.ts`（C2，**Shim 已换 1.2 真类 `UnsupportedCapability`，全仓零残留**）·`wiring/hostSkills.ts`（hma 1.2）·`wiring/hooksYaml.ts`（hma 1.4）——全部在 HEAD 且随测绿。

## 五、全量门禁实跑（审查席复跑，非转抄）

| 门禁 | 结果 |
|---|---|
| 窗口内六测试文件（ledger/lock/broker/context-event/hermeshook/hermes） | ✅ **29 pass / 0 fail** |
| 窗外在册抽测（verify-target/schema-defaults/contract-adapter/artifact-snapshot/caps/types） | ✅ **28 pass / 0 fail** |
| `check-ledger.mjs` 双提案 | ✅ 可跑通（exit 1 = 已知历史债 4 项，见 §三） |
| `summarize-reviews.mjs` 双 change | ✅ 可跑通，exit 0 |
| Docker 全链 verify / e2e | ⏸ 按既留痕替代口径（多日既存状态，非本夜新增缺口） |
| `git status` | ✅ 干净（仅他审查席 `2026-09-14-test.md` untracked，按协议留汇总席统一提交；本报告同） |

## 六、总结论

**🟡 警告通过（不驳回任何任务）**——窗口内 7 任务 DoD 核心条款全部产物实证、57 用例实跑绿，台账/sha/脚本三方对账通过，前夜在册产物零丢失。两项 🟡（1.7 的 R5-b 子声明账实不符、1.3 损坏读入半边未测）均为低成本闭环项，移交汇总席裁定并排下夜；不阻塞今夜定稿。
