# 审查报告·红线七条（席6·2026-09-15）

- **审查范围**：2026-09-14 22:30 后全部改动，共 **16 笔提交**——local-capability-plane 1.6（d3966d7）/ 5.5（a1eef5d）/ 4.1（9934dee）/ 4.2（bc8daa1）/ C4（fdb9113 + lint 修复 8cdbb35 + 未用导入清理 52d5506）/ C5（1de027d）+ host-matrix-alignment 1.8（d4c6d41）+ 各随行 docs sha 定稿 ×8。**口径勘误**：任务书所列 C3 跨库尾注修复（54bd4f6）落在 09-14 08:02，**在本窗之前**（上日已合），未入本窗；但按指令仍对其做了实现正确性复核（见重点复核 A）。
- **权威条文**：[design.md §一降级纪律/§七测试口径/§8.5 S10](../design.md)；host-matrix [tasks.md](../../host-matrix-alignment/tasks.md) 头部纪律行（测试随行 · 账实同步 · 只碰自己条目 E-10 · **协议零变化** · **零触碰 apps/api**）；审查清单 templates.md §四；上夜整改项 R6-a/R6-b/R6-c 已由 4f1f044（09-14 06:06）清零，本窗核其未回退。
- **审查方式**：只读，零 git 写操作。逐笔 `git log --since` + `git show --stat` + 新增源文件全读（export.ts / check-ledger.mjs / bench.ts / bench-cli.mjs）+ 窗口 5 个测试文件夹具逐核 + check-ledger.mjs 实跑 + 实跑取证（窗口 5 文件 **19/19**；CLI 全量 **199 pass / 0 fail / 14 todo**，todo 为既有场景壳）。

## 红线逐条

| # | 红线 | 结论 | 证据 |
|---|---|---|---|
| 1 | 九工具面不变；维护类能力不开 MCP 工具、不设用户命令 | ✅ | 窗口 src 改动仅 capability/export.ts（exportApm/importApm 纯库函数，零命令注册、零 index.ts/USAGE 变更）；participant SKILL.md 未动 |
| 2 | 无常驻进程 | ✅ | bench.ts 为线性单次脚本（mkdtemp 夹具 → 计时 → `rm` 清理 → 自然退出，零 setInterval/setTimeout/Worker/listen）；bench-cli.mjs 用 `spawnSync` 单次等待即退；窗口全量代码正则扫描零定时器/常驻命中 |
| 3 | 零触碰 `apps/api` | ✅ | 窗口 16 笔文件清单仅 `openspec/changes/`、`packages/cli/`、`scripts/bench/` 三处；`git diff d3966d7^..HEAD -- apps/api packages/protocol docs/protocols` 为空 |
| 4 | Zero-LLM | ✅ | 零模型调用、零网络请求（测试 CTX 的 base_url 仅作 provenance 字符串落库，无 fetch）；窗口代码扫描零 `fetch/anthropic/openai` 命中 |
| 5 | 写只碰自己的条目（E-10）；夹具全临时目录 | ✅ | 夹具逐核：capability-export（`mkdtemp` as-export-）、contract-degrade（纯内存 fake 零 IO）、s02/s03（`mkdtemp` as-sc02-/as-sc03- + `AGENTSIGNAL_CONFIG` 覆盖 + after 清理）、wiring-hermes 1.8 补用例（`.hermes` 建在临时 root 内，`HERMES_HOME` env 覆盖，env 删除）——零用例触碰真实宿主配置/真实 `~/.agentsignal`。export 写调用方指定 outDir（测试均指向临时 root 内）。⚠ 加固备案见 🟡-2（importApm 路径拼合无收容校验） |
| 6 | 不引入微服务 / K8s / 消息队列 | ✅ | 窗口零 package.json 变更、零新依赖 |
| 7 | CLI 命令面变更同 PR 同步 participant SKILL | ✅（N/A） | 零命令面变更，G1–G4 不触发 |

## 重点复核 A：C3 跨库尾注解析修复（54bd4f6，窗前提交）——实现正确性确认

- **修复前缺陷坐实**：旧逻辑 `spec[1].startsWith(change)` + 全串 `split(/[\s,]+/)`——对 4f1f044 的双库尾注 `(spec: local-capability-plane 1.4,1.6; host-matrix-alignment 1.2,1.7)`：审 host-matrix 时 `startsWith` 不命中 → 1.2/1.7 全部误报「勾了没提交」；审 local-capability-plane 时平面切分把对侧 1.2/1.7 串库吸入。
- **修复后正确**：`split(";")` 逐段 + 段首 token 与 change **全等**匹配（`!==` continue，前缀误配也不会发生），任务号归属各自 change。**实跑取证**：`node scripts/check-ledger.mjs` 现行输出 host-matrix-alignment 19 提交、1.2/1.7 零误报——4f1f044 豁免注记场景不再误报，✅。
- **遗留瑕疵（⚪-1）**：commitsFor 末尾**重复两行一模一样的 `return`**（check-ledger.mjs:65–66，修复笔引入的不可达死代码；scripts/ 在 biome lint 范围外故未拦）。功能无害，建议汇总席顺手删一行。

## 重点复核 B：S10 零泄漏断言 + 勾选一致性

| 检查项 | 结论 | 证据 |
|---|---|---|
| S10 出口侧零泄漏（4.1） | ✅ 有断言保障 | export.ts 的 apm.yml 由**固定白名单行**拼装（name/version/skills 的 name+path，无时间戳确定性输出）；capability-export.test.ts:79–88 逐键断言 apm.yml **不含** `triggers/layers/domains/keywords/provenance/agentsignal.vip`；:89–93 断言产物 SKILL.md 无 provenance/layers |
| S10 往返无损 + 私有不回转（4.2） | ✅ | importApm 私有层以安全默认重建（domains common/layers base/triggers=keyword 包名），**不从产物回转**；test.ts:130–154 deepEqual 断言默认 triggers 形态 + 正文逐字往返；origin={kind:"apm-import", ref:包名} 溯源断言在 |
| 勾选一致 | 🟡（一项偏差，见 🟡-1） | 4.1/4.2/5.5/C4/C5/host-matrix 1.8 的 feat ↔ tasks.md [x] 一一对应；**例外**：1.6 尾注超宣称——d3966d7 尾注写 `(spec: local-capability-plane 1.6)`，但 tasks.md **1.6 = apm 适配器 spawn**（未动、未勾），该笔实做（降级矩阵首条夹具 + S6 + fake 过 C2 套件）属 **1.7** 范围且 1.7 同样未勾。check-ledger 实跑现行即报 `[2 提交没勾] 1.6` |
| 工作区 | ✅ | `git status --porcelain` 与 `git diff HEAD --stat` 均为空（本报告文件为审查席设计内产物，留汇总席统一提交） |
| 实跑 | ✅ | 窗口 5 测试文件 19/19；CLI 全量 199/0/14 todo 全绿 |

## 🟡 整改 / 备案项（不构成红线违反，按序处置）

1. **R7-a（限次夜汇总席修，账实同步 §10）**：1.6 尾注超宣称——d3966d7 的 spec 尾注应更正为 1.7（或在 tasks.md 就 1.7 落勾/留「部分完成」注记），tasks.md 1.6（apm 适配器）保持未勾不动。现状 check-ledger 已现实报错，留着即每次对账噪音。
2. **R7-b（P2 加固备案，低危）**：importApm 将外部 apm.yml 的 `entry.name`/`entry.path` 直接 `path.join` 进落盘/读取路径，无收容校验（如 `../` 形态可越出 skillsDir/inDir）。当前输入契约是「自家导出或同构可信包」故不构成违反，但 4.2 面向三方 APM 包，建议 P2 对账器落地时补 resolve 后前缀断言（对照上夜 hostSkills.ts 白名单防穿越先例）。
3. **⚪-1（代码卫生）**：check-ledger.mjs:65–66 重复不可达 `return`（窗前 54bd4f6 引入），删一行即可；scripts/ 不在 biome 范围，勿指望 lint 拦。
4. **备案（非本窗引入）**：check-ledger 对 audit-restore / skill-engine / user-domain-completion / ux-foundation 等旧 change 报的批量「1 勾没提交」系尾注通约确立前的历史提交形态（0.x 调研类任务无独立 spec 尾注提交），属脚本已知局限非新违规；ux-foundation 另有 4 笔「3 无台账条目」待其 own 线处置，均不入本窗结论。

## 总结论

**🟢 通过（附两项非阻塞移交）**——红线七条当夜零违反：协议零变化、零触碰 apps/api、Zero-LLM、无常驻/定时器、零新依赖、命令面零变化、夹具全临时目录全部坐实（CLI 全量 199/199 实跑绿，窗口 19/19）；S10 零泄漏有逐键断言保障、往返无损成立；C3 分号分段修复实跑确认 4f1f044 场景不再误报（该修复在窗前，非本窗产物）。移交：🟡-1 台账尾注超宣称（汇总席当场修）、🟡-2 importApm 路径收容（P2 加固）、⚪-1 死代码一行（顺手清）。

—— 审查席 6（红线）· 2026-09-15 · 只读审查，零 git 写操作
