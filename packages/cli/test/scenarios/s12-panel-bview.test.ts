/**
 * 场景 S12：面板扩展（轨道 C · C1 场景壳，待 P 阶段实现后填充）。
 *
 * 前置：B 视图 + 夹具数据
 * 通过标准：四新区块数据断言 · 全站无「skills」· 降级落档可见
 * 验证层：V1
 * 填充注记：依赖 P7 7.4 面板四区落地后填充
 *
 * fixture 库：./fixtures.ts（makeScenarioRoot / seedHostSkillsDir / seedForeignSkill / seedTwoHosts）。
 * 纪律：不碰真实宿主配置；一切落临时目录；外部交互 fake/隔离。
 */
import { test } from "node:test";

test.todo("S12 面板扩展：待对应 P 阶段实现后填充断言");
