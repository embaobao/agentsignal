/**
 * 场景 S13：运行时拉起（轨道 C · C1 场景壳，待 P 阶段实现后填充）。
 *
 * 前置：git / npm 包源
 * 通过标准：按需拉起 Agent → 用完即退（无常驻）；账本记条目
 * 验证层：V2
 * 填充注记：依赖 P8 8.3 运行时拉起落地后填充
 *
 * fixture 库：./fixtures.ts（makeScenarioRoot / seedHostSkillsDir / seedForeignSkill / seedTwoHosts）。
 * 纪律：不碰真实宿主配置；一切落临时目录；外部交互 fake/隔离。
 */
import { test } from "node:test";

test.todo("S13 运行时拉起：待对应 P 阶段实现后填充断言");
