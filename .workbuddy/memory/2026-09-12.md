# 2026-09-12

## 夜间轮 2（通用轨，00:00–00:40）

- Docker 仍未起 → skill-engine 完成验证闭环 + audit-restore（均需 PG）连续留痕顺延。
- **host-matrix-alignment 1.1+1.5**（e7fc8f2）：Hermes 打底——HostId 加 hermes · HostDef 增 soulPath/hooksYamlPath/hookAllowlistPath · mcpPath 放宽 `string|null`（D1/D7）· snippets wire/status/unwire + toml 族 + extra 族 null 守卫（零垃圾断言：wireMcp(hermes) 不产生任何文件）· **protocol `HostBindingSchema.host` 枚举加 hermes**（三提案协调点打底首步，config 真源闭环）；wizard 断言宿主数 5→6；新测试 wiring-hermes.test.ts 6 用例，CLI 117/117。
- **2.4+2.5**（480c128）：hosts.ts 头注释清偿——删 Trae 漂移（提案授权夜间保守执行「删注释」留档可翻案）+ 写入 D7 不猜 MCP 纪律。
- **下一轮**：01:00 席 2 接 local-capability-plane 轨道 B（P5 5.1 起，会改 protocol skill-schema.ts verify_target 段——与本轮 HostBinding 枚举不同区域，无冲突）；通用轨后续可取 host-matrix 1.2/1.3（skills 落盘 + SOUL.md 块注入）。
