# 2026-09-12

## 夜间轮 2（通用轨，00:00–00:40）

- Docker 仍未起 → skill-engine 完成验证闭环 + audit-restore（均需 PG）连续留痕顺延。
- **host-matrix-alignment 1.1+1.5**（e7fc8f2）：Hermes 打底——HostId 加 hermes · HostDef 增 soulPath/hooksYamlPath/hookAllowlistPath · mcpPath 放宽 `string|null`（D1/D7）· snippets wire/status/unwire + toml 族 + extra 族 null 守卫（零垃圾断言：wireMcp(hermes) 不产生任何文件）· **protocol `HostBindingSchema.host` 枚举加 hermes**（三提案协调点打底首步，config 真源闭环）；wizard 断言宿主数 5→6；新测试 wiring-hermes.test.ts 6 用例，CLI 117/117。
- **2.4+2.5**（480c128）：hosts.ts 头注释清偿——删 Trae 漂移（提案授权夜间保守执行「删注释」留档可翻案）+ 写入 D7 不猜 MCP 纪律。
- **下一轮**：01:00 席 2 接 local-capability-plane 轨道 B（P5 5.1 起，会改 protocol skill-schema.ts verify_target 段——与本轮 HostBinding 枚举不同区域，无冲突）；通用轨后续可取 host-matrix 1.2/1.3（skills 落盘 + SOUL.md 块注入）。

## 夜间轮 3（席2·轨道B，01:00–01:55）

- **P5 5.1**（f76f666）：verify_target 补实现——protocol 新增 SkillVerifyTargetSchema({statement,checks[]})；frontmatter 字段收宽 union（新对象/旧 null/旧数组全兼容）；verify.ts normalizeVerifyTarget 引擎层归一（旧数组→statement=首条，空→null）；VerifyResult 带归一目标（三态判定可关联）；SkillRecord 类型透传。踩坑记录：CLI 经 @agentssignal/protocol 走 **dist 产物**，schema 改动必须重建 dist 再跑测试（源码引测试 ≠ 运行时引 dist）。
- **P5 5.2**（2653873）：provenance.origin 补写入——TranscodeInput 增 origin 声明，缺省 {kind:"platform",ref:sig_id}（新落库必有 origin），包导入/手动留 P4 显式透传位；golden 快照随新语义更新。
- **门禁**：CLI 126/126 · check/lint 绿 · 已 push。轨道 B 进度 5.1/5.2 ✅，5.3（三计数分立）留下一轮。

## 夜间轮 4（通用轨，02:00–02:40）

- Docker 仍未起（①skill-engine 闭环 + ②audit-restore 连续留痕顺延——已连续 4 轮，OrbStack 整夜未开）。
- **host-matrix-alignment 1.3**：SOUL.md 块注入（wiring/soul.ts 幂等 create-or-append + 摘除只删标记段）+ 接线 writeExtra/removeExtra hermes 分支（rules 兜底注入 SOUL.md）；零垃圾断言升级为「HERMES_HOME 仅 SOUL.md、无 MCP 文件」；CLI 132/132。
- 下一轮：03:00 席 3（轨道 C 基建轨：C1 场景壳/C2 契约套件/C3 台账脚本）；通用轨后续接 host-matrix 1.2（skills 落盘，需先理 init 落盘路径）。

## 夜间轮 5（席3·轨道C，03:00–03:55）

- **C1**（b31187d）：test/scenarios/ 16 壳（S1–S16 逐条注记前置/通过标准/验证层/填充依赖）+ fixtures.ts 六构造器；S8 fakeAdapter 标注待 P1 types。
- **C3**（59e7834）：scripts/check-ledger.mjs 四类对账（任务号主键，sha 失效=warning）；**发现并修正流程悖论**——台账 sha 取于 amend 前必然失效，改「下一笔回填最终 sha」约定；顺手修正 LCP/host-matrix 两份台账 9 个废弃 sha；首跑暴露 host-matrix 0.1–0.4 历史无尾注勾选（真实债留审查席）。
- **⚠️ 待办（下轮首笔）**：LCP progress.md 的 C3 行 sha 占位仍是 amend 前值，修正为 `59e7834`。
- Docker 连续第 5 轮未起。下一轮：04:00 审查三席槽（本自动化是单会话按 P-2 降档：单席 spawn 3 只读子代理并行审查）。

## 夜间轮 6（通用轨，04:00–04:40）

- **host-matrix-alignment 1.4**（b905829）：hook 双写——wiring/hooksYaml.ts（config.yaml 行级 hooks.<event>[] 注入/摘除 + allowlist.json 去重，tmp+rename，按 (event,command) 幂等）；修掉首版幂等粒度 bug（按 command 判重会漏同命令多 event 场景，红测当场抓出）；接线整合与 event 映射归 1.6/1.7。
- **台账 sha 稳态**：C3 行最终 sha 修正为 59e7834；1.4 台账行 sha 已回填 b905829（先提交后回填独立修正笔的约定初见成效——本轮用「提交后 rev-parse 回填+下轮修正」过渡）。
- Docker 连续第 6 轮未起。下一轮 05:00：审查三席槽（P-2 降档单席三子代理并行审查）。
