# 2026-09-11

## 两条 session 线进度打通（本文件即打通点）

本日有两条并行工作线，产出曾互相矛盾，现对齐如下。

### 线 A ·「想法」线（外部调研归档）

- 归档 `docs/notes/2026-09-11-mcp-hosting-management-trio.md`：MCP 托管/网关/管理三件套调研（microsoft/mcp-gateway · xingkongliang/skills-manager · samanhappy/mcphub）。
- 三问结论：① **MCP 动态加载不自建网关**（三个现成轮子已熟、形态与我们冲突），落法 =「外挂兼容」；② skill 动态加载消费侧已落地；③ **真正值得自建的空白位 =「fork 远程 Agent」**——把一组已验证经验一键物化成临时执行环境，三家都没做，是差异位。
- 建议：不做自建网关；小步做 wiring「MCP 端点透传」；中步立项 `fork` 编排（agent-preset），排队 D/E 后。

### 线 B ·「构建」线（本 session）

- 归档 `docs/notes/2026-09-10-skill-management-landscape.md`：分层 Skill 方案（构想件）+ skills-manager 调研。结论：不引入形态，吸收四点概念。
- **照出缺口**：经验落库 `~/.agentsignal/skills/` ⇄ 宿主技能目录之间断裂（有代码为证）。
- 立提案 `openspec/changes/host-integration/`（四件套）：经验落地 × 反向可见 × 状态治理；核心支点 = 落地是**稀缺动作**（准入控制），默认关闭。
- 另核实本地化闭环可用：本地 `apps/api` + 两个开关（`SELF_REGISTER_ENABLED=1`、`AGENTSIGNAL_BASE_URL=http://localhost:3000`）即可跑内部线闭环；`dynamic-skill-management` 已完成并归档。

### 冲突与勘误（本次打通的核心动作）

- **冲突**：线 A 笔记 §四 曾断言「落库 ≠ 宿主可见」断裂**已由 wiring 接线接通**——**错误**。
- **勘误**（已就地修正线 A 笔记 §四）：`wiring/snippets.ts` 只写 MCP 配置 / hook / rules 一行三类；`wiring/hosts.ts` 的 `HostDef` 无 skills 目录位；`installSignal` 只写中央库。**宿主原生可见仍未接通**，正是线 B 的 host-integration 所补。
- **概念澄清**：`search_skills` 可达（MCP 按需拉）≠ 宿主原生可见（技能目录常驻）。两者是权衡关系，不是同一件事。

### 两条线的依赖关系（串起来）

```text
订阅落库（已上线）→ MCP 按需消费（已上线）
                    ↓ 缺
        宿主技能目录落地（host-integration 提案）
                    ↓ 依赖
        批量物化能力
                    ↓ 支撑
        fork 远程 Agent 临时执行环境（trio 建议立项）
```

即：trio 的 `fork` 构想若要"把一组经验物化到宿主"，**需要先有 host-integration 的装载能力**；两提案是同一条链的上下段，不是并列的两个新方向。

### 待站长裁决

1. host-integration 的立论应从"补缺口"改为"权衡+裁决点"（是否付常驻 context 成本换"无需主动检索"），并补 validation 预登记（见 review 结论）。
2. fork（agent-preset）是否立项，及其与 host-integration 的先后。
3. wiring「MCP 端点透传」条目是否并入 host-matrix-alignment 顺手做。

### 线 C ·「内容/暴露」线（同日在另一 session 新立）

- 新增提案 `openspec/changes/mcp-service-exposure/`：本地 MCP 服务暴露（宿主子进程 → 可选服务端点）。范围只解「供给面」，不做零成本托管。其 §三.0 P0 前置修复① 与本线核实一致：**环境变量名分裂**——`mcp/client.ts:19` 读 `AGENTSIGNAL_BASE_URL`，而 `index.ts:38` / `mirror.ts:38` 读 `AGENTSIGNAL_BASE`。
- 新增提案 `openspec/changes/local-content-model/`：内容模型细化 × 本地承载与验证（名词四层 Topic / Signal / Experience / Skill(内部形式) + 分析/同步/承载/验证四边界）。
- 归档：根级 `agent-skill-layered-management.md` → `docs/notes/2026-09-11-agent-skill-layered-management.md`（治理 §1 合规；根目录已清）。

### ⚠️ 三提案同改 `HostDef` 的协调点（本次打通的关键风险）

| 提案 | 对 `packages/cli/src/skills/wiring/hosts.ts` 的改动 |
|---|---|
| host-matrix-alignment | `HostId` 5→9；`HostDef` 增 `soulPath` / `hooksYamlPath` / `hookAllowlistPath` / `userScope` / `instructionsManifest` |
| host-integration（线 B） | `HostDef` 增 `skillsPath` / `skillLayout` |
| mcp-service-exposure | `HostDef` 增「MCP 端点透传」条目 |

三者扩同一个接口 → **必须先约定 `HostDef` 一次扩全，避免三次独立改动互踩**；顺序建议：host-matrix-alignment 打底 → host-integration / mcp-service-exposure 各自只加字段。
另：`packages/protocol/src/skill-schema.ts` 的 `HostBindingSchema.host` 是 **5 宿主硬编码枚举**，三提案都依赖其扩展（最易被漏）。

### 文档死链体检与修复（2026-09-11 本线执行）

- 活文档区相对链接死链**已全部修复**：`docs/design/glossary.md`（`decisions/`→`../decisions/`、`design/x`→同目录、`protocols/`→`../protocols/`）· `implementation-tasks.md` / `local-engine.md` / `roadmap.md`（`decisions/`→`../decisions/`；`dynamic-skill-management`→`archive/`）· `docs/README.md` · `openspec/changes/{human-auth-providers,user-domain-completion,skill-engine}/` 的层级错误。
- 复扫结果：**活文档区 0 死链**。
- **归档件不改**（`openspec/changes/archive/`、`docs/decisions/`、`docs/notes/`）：按治理 §6「ADR 与 notes 归档文本不回写」，其链接因目录移动而失效属历史状态，不回写。
